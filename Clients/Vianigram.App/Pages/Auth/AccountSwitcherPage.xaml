<Page
    x:Class="Vianigram.App.Pages.Auth.AccountSwitcherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Vianigram.App.ViewModels.Pages"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:AccountSwitcherPageViewModel, IsDesignTimeCreatable=True}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="AccountRowTemplate">
            <Grid Margin="16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="44" Height="44"
                        CornerRadius="22"
                        Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <TextBlock Text="{Binding AvatarLetter, Mode=OneWay}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DisplayName, Mode=OneWay}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding PhoneNumber, Mode=OneWay}"
                               FontSize="13"
                               Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                               Opacity="0.6"
                               Margin="0,2,0,0" />
                </StackPanel>

                <TextBlock Grid.Column="3"
                           Text="&#x2713;"
                           FontSize="20"
                           Foreground="{ThemeResource SystemControlBackgroundAccentBrush}"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsActive, Mode=OneWay}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                Padding="16,14">
            <TextBlock Text="Accounts"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                       VerticalAlignment="Center" />
        </Border>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Accounts}"
                  ItemTemplate="{StaticResource AccountRowTemplate}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OnAccountItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Button Grid.Row="2"
                Content="Add account"
                Command="{Binding AddAccountCommand}"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Left"
                Padding="16,14" />
    </Grid>
</Page>
