<Page
    x:Class="Vianigram.App.Pages.Settings.ScheduledPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Vianigram.App.ViewModels.Pages"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:ScheduledPageViewModel, IsDesignTimeCreatable=True}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Scheduled Messages"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                   Margin="20,12,20,8" />

        <Grid Grid.Row="1">

            <ListView ItemsSource="{Binding Messages}"
                      SelectionMode="None"
                      IsItemClickEnabled="False">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="16,8,16,8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="{Binding ScheduledAtText}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{ThemeResource SystemControlForegroundAccentBrush}" />

                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="{Binding Preview}"
                                       FontSize="15"
                                       Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       MaxLines="2"
                                       Margin="0,2,0,0" />

                            <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                <Button Content="Send"
                                        FontSize="12"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                                        CommandParameter="{Binding}"
                                        Command="{Binding DataContext.SendNowCommand, ElementName=PageRoot}" />
                                <Button Content="Edit"
                                        FontSize="12"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                                        CommandParameter="{Binding}"
                                        Command="{Binding DataContext.EditCommand, ElementName=PageRoot}" />
                                <Button Content="Delete"
                                        FontSize="12"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="#FFFF5252"
                                        CommandParameter="{Binding}"
                                        Command="{Binding DataContext.DeleteCommand, ElementName=PageRoot}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Text="No scheduled messages"
                       FontSize="14"
                       Opacity="0.6"
                       Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="{Binding EmptyVisibility, Mode=OneWay}" />

            <ProgressRing IsActive="{Binding IsLoading, Mode=OneWay}"
                          Width="32" Height="32"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>
