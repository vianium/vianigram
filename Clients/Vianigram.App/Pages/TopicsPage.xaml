<Page
    x:Class="Vianigram.App.Pages.TopicsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Vianigram.App.Controls"
    xmlns:vm="using:Vianigram.App.ViewModels.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:TopicsPageViewModel, IsDesignTimeCreatable=True}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Create"
                          Icon="Add"
                          Command="{Binding CreateTopicCommand}" />
        </CommandBar>
    </Page.BottomAppBar>

    <Page.Resources>
        <DataTemplate x:Key="TopicRowTemplate">
            <Grid Margin="14,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="44" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="40"
                        Height="40"
                        CornerRadius="20"
                        Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding IconEmoji}"
                               FontSize="20"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White" />
                </Border>

                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding LastMessagePreview}"
                               FontSize="13"
                               Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                               Opacity="0.6"
                               TextTrimming="CharacterEllipsis"
                               Margin="0,2,0,0" />
                </StackPanel>

                <controls:UnreadBadge Grid.Column="3"
                                      Count="{Binding UnreadCount}"
                                      VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                Padding="16,14">
            <StackPanel Orientation="Horizontal">
                <controls:AvatarCircle AvatarSize="36"
                                       Initials="{Binding AvatarLetter, Mode=OneWay}"
                                       VerticalAlignment="Center" />
                <TextBlock Text="{Binding ForumTitle, Mode=OneWay}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0" />
            </StackPanel>
        </Border>

        <!-- Topic list -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding Topics}"
                  ItemTemplate="{StaticResource TopicRowTemplate}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="OnTopicItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   Text="No topics yet"
                   Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                   Opacity="0.6"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsEmpty, Mode=OneWay}" />
    </Grid>
</Page>
